parse scene :
    Fill camera struct
    Fill objects struct (can use only one with identifier)
    Fill lights struct
    Fille hte scene struct adding ambient

main :
    for each pixel :
        compute ray
        compute intersection
        compute color
        put color in image
    save image
    free all


compute ray :
    create ray from camera position to pixel position
    check if ray intersect with any objects
    if no intersection return background color
    else 
        get intersection point (closest one)
        compute color

compute color :
    voir phong model doc (chapter 3 only for bonus)
    

